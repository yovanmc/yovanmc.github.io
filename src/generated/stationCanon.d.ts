/**
 * Hand-written declarations for the generated canon module (see stationCanon.js
 * header; regenerated by tools/extract-canon.mjs, verified by npm run verify:canon).
 */

/**
 * The locked station v1 canon (2026-07-27). Returns a complete standalone
 * `<svg viewBox="-510 -510 1020 1020">` string sized 100%×100% to its container.
 * Pass a non-empty `idSuffix` when a second copy shares the same document, so the
 * internal defs ids (fcL/skL/wtL) don't collide.
 */
export declare function buildStationCanon(idSuffix?: string): string;
